<resources xmlns:tools="http://schemas.android.com/tools">
    <style name="Theme.App.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">#FFFFFF</item>
        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_launcher</item>
        <item name="postSplashScreenTheme">@style/Theme.Tarea2_ManjonCarrascoJoseCarlos</item>
    </style>
    <!-- Tema principal de la aplicación -->
    <style name="Theme.Tarea2_ManjonCarrascoJoseCarlos" parent="Base.Theme.Tarea2_ManjonCarrascoJoseCarlos">
        <!-- Overlay para que el Toolbar use estilo oscuro sobre colorPrimary -->
        <item name="toolbarStyle">@style/Widget.Material3.Toolbar</item>
    </style>

</resources>